const styles = theme => ({
  root: {
    flexGrow: 1,
    height: "100%",
    width: "100%"
  },
  button: {
    margin: theme.spacing.unit
  },
  paper: {
    padding: theme.spacing.unit
  }
});

export default styles;
